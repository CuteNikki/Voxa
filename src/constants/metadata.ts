import { Metadata } from 'next';

export const defaultMetadata: Metadata = {
  title: 'Voxa — Real-time Chat',
  description: 'Real-time Chat Platform with public channels, DMs, and image support',
};
